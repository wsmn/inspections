<% content_for(:sidemenu) do %>
  <%= render('panel', active: :new) %>
<% end %>

<div class="w-full -mt-4">
	<h1 class="text-3xl font-bold text-blue-900 mb-4"><%= t('.title') %>  <i class="ml-2 far fa-sm fa-folder-open"></i></h1>
	<%= render('form', project: @project) %>  
</div>
