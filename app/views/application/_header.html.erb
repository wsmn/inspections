<nav class="navbar">
  <div class="navbar-brand">
    <%= link_to(root_path, class: 'navbar-item') do %>
      <%= image_tag('ka.svg', style: 'width: 8rem;') %>
    <% end %>
    <div class="navbar-burger burger" data-target="navMenu">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <div id="navMenu" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to(Project.model_name.human(count: 2),
                  projects_path,
                  class: navbar_is_active('navbar-item is-tab', 'projects'),
                  data: { turbolinks: false }) %>
      <div class="navbar-item has-dropdown is-hoverable">
        <%= link_to(Customer.model_name.human(count: 2),
                    customers_path,
                    class: navbar_is_active('navbar-item is-tab', 'customers'),
                    data: { turbolinks: false }) %>
        </div>
        <div class="navbar-item has-dropdown is-hoverable">
          <%= link_to(TimeEntry.model_name.human(count: 2),
                      time_entries_path,
                      class: navbar_is_active('navbar-item is-tab', 'timeEntries'),
                      data: { turbolinks: false }) %>
          </div>
      </div>
        </div>
      </div>
    </div>
  </div>
</nav>


<!-- <nav class="navbar has-shadow">
    <div class="navbar-brand">
      <%= link_to(root_path, class: 'navbar-item') do %>
        <%= image_tag('ka.svg', style: 'width: 8rem;') %>
      <% end %>
      <%= link_to(Project.model_name.human(count: 2),
                  projects_path,
                  class: navbar_is_active('navbar-item is-tab is-hidden-mobile', 'projects'),
                  data: { turbolinks: false }) %>
      <%= link_to(Customer.model_name.human(count: 2),
                  customers_path,
                  class: navbar_is_active('navbar-item is-tab is-hidden-mobile', 'customers'),
                  data: { turbolinks: false }) %>
    </div>
    <span class="navbar-toggle" id=toggle>
      <span></span>
      <span></span>
      <span></span>
    </span>
    <div class="navbar-right navbar-menu">
      <%= link_to(Project.model_name.human(count: 2),
                projects_path,
                class: navbar_is_active('navbar-item is-tab is-hidden-tablet', 'projects'),
                data: { turbolinks: false }) %>

      <%= link_to(Customer.model_name.human(count: 2),
                customers_path,
                class: navbar_is_active('navbar-item is-tab is-hidden-tablet', 'customers'),
                data: { turbolinks: false }) %>
    </div>
</nav> -->
