<nav class="navbar">
  <div class="navbar-brand">
    <%= link_to(root_path, class: 'navbar-item') do %>
      <%= image_tag('ka.svg', style: 'width: 8rem;') %>
    <% end %>
    <div class="navbar-burger burger" data-target="navMenu">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <div id="navMenu" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to(Home.model_name.human(count: 2),
                  root_path,
                  class: navbar_is_active('navbar-item is-tab is-hidden-desktop', 'home')) %>
      <%= link_to(Project.model_name.human(count: 2),
                  projects_path,
                  class: navbar_is_active('navbar-item is-tab is-hidden-desktop', 'projects')) %>
      <div class="navbar-item has-dropdown is-hoverable">
        <%= link_to(Customer.model_name.human(count: 2),
                    customers_path,
                    class: navbar_is_active('navbar-item is-tab is-hidden-desktop', 'customers')) %>
      </div>
      <div class="navbar-item has-dropdown is-hoverable">
        <%= link_to(TimeEntry.model_name.human(count: 2),
                    time_entries_path,
                    class: navbar_is_active('navbar-item is-tab is-hidden-desktop', 'time_entries')) %>
      </div>
    </div>
</nav>
