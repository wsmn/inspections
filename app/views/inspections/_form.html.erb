<%= simple_form_for(inspection) do |f| %>
  <%= f.association(:customer, collection: Customer.all) %>
  <%= f.input(:on, as: :datepicker) %>
  <%= bulma_button(f) %>
<% end %>

<%= javascript_pack_tag('forms') %>
<%= stylesheet_pack_tag('forms') %>
