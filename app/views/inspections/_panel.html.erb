<% active ||= :none %>
<nav class="panel">
  <%= active_panel(inspections_path, active, :index) do %>
    <span class="panel-icon">
      <%= fa_icon('list') %>
    </span>
    <%= Inspection.model_name.human(count: 2) %>
  <% end %>

  <%= active_panel(new_inspection_path, active, :new) do %>
    <span class="panel-icon">
      <%= fa_icon('plus-square') %>
    </span>
    <%= t('.new') %>
  <% end %>
</nav>
