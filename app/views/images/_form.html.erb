<%= simple_form_for([project, image]) do |f| %>
  <%= image_tag(image.file, class: 'image is-128x128') if image.file.attached?%>
  <%= f.input(:file) %>
  <%= f.input(:description) %>
  <%= bulma_button(f, parent: project) %>
<% end %>
